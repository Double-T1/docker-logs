[
  {
    "name": "logger",
    "image": "${AWS_ACCOUNT_ID}.dkr.ecr.ap-southeast-1.amazonaws.com/${PROJECT_FULL_NAME}-logger:latest",
    "cpu": 256,
    "memory": 512,
    "portMappings": [
      {
        "name": "logger-8000-tcp",
        "containerPort": 8000,
        "hostPort": 8000,
        "protocol": "tcp"
      }
    ],
    "essential": true,
    "readonlyRootFilesystem": false,
    "environment": [
      {
        "name": "ENV",
        "value": "${ENVIRONMENT}"
      },
      {
        "name": "AWS_REGION",
        "value": "ap-southeast-1"
      },
      {
        "name": "LOG_LEVEL",
        "value": "DEBUG"
      }
    ],
    "secrets": [],
    "mountPoints": [],
    "volumesFrom": [],
    "logConfiguration": {
      "logDriver": "awslogs",
      "options": {
        "awslogs-group": "/ecs/${PROJECT_FULL_NAME}-logger",
        "awslogs-create-group": "true",
        "awslogs-region": "ap-southeast-1",
        "awslogs-stream-prefix": "ecs"
      }
    },
    "systemControls": []
  }
]
